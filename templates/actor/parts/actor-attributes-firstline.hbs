{{!-- "flex-group-center" is also defined in the _grid.scss file and it will add a small amount of padding, a border, and will center all of its child elements content and text. --}}
<div class="resource flex-group-center">
    <label class="resource-label resource-name {{#if (isDiceRollable system.attributes.flashlights.value)}}rollable{{/if}}" data-resource="flashlights">{{localize "CTHACK.FlashLights"}}<em>{{localize "CTHACK.FlashLightsDesc"}}</em></label>
    <div class="resource-content">
        <select name="attributes.flashlights.value" data-type="String" {{{getStyleForDice system.attributes.flashlights.value}}} >
        {{#select system.attributes.flashlights.value}}
            {{> "systems/cthack/templates/parts/dice-sides-options-value.hbs"}}
        {{/select}}
        </select>
        <span>/</span>
        <select name="attributes.flashlights.max" data-type="String" {{{getStyleForDice system.attributes.flashlights.max}}} >
        {{#select system.attributes.flashlights.max}}
            {{> "systems/cthack/templates/parts/dice-sides-options-max.hbs"}}
        {{/select}}
        </select>
    </div>
</div>                  
<div class="resource flex-group-center">
    <label class="resource-label resource-name {{#if (isDiceRollable system.attributes.smokes.value)}}rollable{{/if}}" data-resource="smokes">{{localize "CTHACK.Smokes"}}<em>{{localize "CTHACK.SmokesDesc"}}</em></label>
    <div class="resource-content">
        <select name="attributes.smokes.value" data-type="String" {{{getStyleForDice system.attributes.smokes.value}}} >
        {{#select system.attributes.smokes.value}}
            {{> "systems/cthack/templates/parts/dice-sides-options-value.hbs"}}
        {{/select}}
        </select>
        <span>/</span>
        <select name="attributes.smokes.max" data-type="String" {{{getStyleForDice system.attributes.smokes.max}}} >
        {{#select system.attributes.smokes.max}}
            {{> "systems/cthack/templates/parts/dice-sides-options-max.hbs"}}
        {{/select}}
        </select>
    </div>
</div>
<div class="resource flex-group-center">
    <label class="resource-label resource-name {{#if (isDiceRollable system.attributes.sanity.value)}}rollable{{/if}}" data-resource="sanity">{{localize "CTHACK.Sanity"}}<em>{{localize "CTHACK.SanityDesc"}}</em></label>
    <div class="resource-content">
        <select name="attributes.sanity.value" data-type="String" {{{getStyleForDice system.attributes.sanity.value}}} >
        {{#select system.attributes.sanity.value}}
            {{> "systems/cthack/templates/parts/dice-sides-options-value.hbs"}}
        {{/select}}
        </select>
        <span>/</span>
        <select name="attributes.sanity.max" data-type="String" {{{getStyleForDice system.attributes.sanity.max}}} >
        {{#select system.attributes.sanity.max}}
            {{> "systems/cthack/templates/parts/dice-sides-options-max.hbs"}}
        {{/select}}
        </select>
    </div>
</div>

<div class="resource flex-group-center">
    {{#if (isEnabled "HitDiceResource")}}
        <label for="hitDice.value" class="resource-label hit-name {{#if (isEnabled "HitDiceResource")}}rollable{{/if}}" data-resource="hitDice">{{localize "CTHACK.HitDice"}}</label>
        <div class="resource-content">
            <select name="attributes.hitDice.value" data-type="String" {{{getStyleForDice system.attributes.hitDice.value}}} >
                {{#select system.attributes.hitDice.value}}
                    {{> "systems/cthack/templates/parts/dice-sides-options-value.hbs"}}
                {{/select}}
            </select>
            <span>/</span>
            <select name="attributes.hitDice.max" data-type="String" {{{getStyleForDice system.attributes.hitDice.max}}} >
                {{#select system.attributes.hitDice.max}}
                    {{> "systems/cthack/templates/parts/dice-sides-options-max.hbs"}}
                {{/select}}
            </select>
        </div>
    {{else}}
        <label class="resource-label">{{localize "CTHACK.HitPoints"}}</label>
        <div class="resource-content flexrow flex-center hitpoints">
            <input type="text" class="input-hitpoints hitpoints-frame" name="hp.value" value="{{system.hp.value}}" data-dtype="Number"/>
            &nbsp;/&nbsp;
            <input type="text" class="input-hitpoints hitpoints-frame" name="hp.max" value="{{system.hp.max}}" data-dtype="Number"/>
        </div>
    {{/if}}

</div>